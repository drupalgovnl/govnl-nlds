{/* label.mdx */}

import {
  Canvas,
  Meta,
  Markdown,
  ArgTypes,
  Title,
  Subtitle,
  Story,
} from '@storybook/addon-docs/blocks';
import * as LabelStories from './label.stories.js';
import Readme from './README.md?raw';

<Meta of={LabelStories} title="Componenten/Form elements/Label" />

<Title>Label</Title>

<Subtitle>
  Een label is beschrijvende tekst die het doel van een form element, zoals een text input of select
  list, communiceert aan de gebruiker.
</Subtitle>

<Canvas of={LabelStories.LabelDefault} sourceState="none" />

## Parameters

<ArgTypes of={LabelStories} />

<Markdown>{Readme}</Markdown>

## Wanneer gebruik je de Label component

Gebruik de Label component voor een beschrijving van een onderdeel van de user interface. In de meeste gevallen is dit een input, textarea of select component in een formulier.

## Richtlijnen

- Wanneer de label gebruikt wordt voor een bijbehorend form element, geef dan door middel van het `for` attribuut een verwijzing mee naar het `id` van het form element.
- Wanneer de labl niet gebruikt wordt voor een form element, dan kan het `for` attribuut achterwege worden gelaten.
- Zet geen heading in het label component. Het is voor screenreaders lastig om dit te intepreteren, waardoor gebruikers van screenreaders niet de juiste informatie krijgen.
- Zet geen interactieve elementen als een link in een label. Een label dat verbonden is aan een form element werkt als een verlengde van dat element. Wanneer je op de label klikt
  geldt dit als een klik op het form element. Deze functionaliteit werkt niet samen met bijvoorbeeld een link in de label.

## Voorbeelden

### Label

<Canvas of={LabelStories.LabelDefault} />

Naast de default variant is er ook een `strong` variant van het label. Deze variant kan bijvoorbeeld gebruikt worden om het uiterlijk van de label gelijk te trekken met het uiterlijk van de legend
element in een fieldset, zodat de gehele flow van een formulier er visueel beter uitziet.

### Strong label

<Canvas of={LabelStories.LabelStrong} />

### Label met een toevoeging voor het verplicht invullen

Als het voor het bijbehorende form element verplicht is om in te vullen, kan dit duidelijk worden gemaakt in het label. Hiervoor kan een `span` element worden toegevoegd met
de className `dictu-label__required`.

<Canvas of={LabelStories.LabelRequired} />

## Relevante WCAG succescriteria

(nog in te vullen)

## Help mee om deze component te verbeteren

- Neem deel aan de discussie over de [Label component op GitHub]().
- [Biedt een wijzigingsvoorstel aan op Github](). Lees hoe je het beste [een wijziging kunt aanbieden]().

## Heb je hulp nodig?

Heb je een vraag over deze component of het DICTU Design System? [Neem dan contact op met het team](?path=/docs/informatie-team--docs).

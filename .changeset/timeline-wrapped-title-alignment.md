---
"@dictu/timeline": patch
---

Fix timeline layout for wrapping titles: keep the item and sub-item icons aligned with the connector line (and with each other) when a title wraps onto multiple lines, and left-align wrapped titles instead of centring them.
